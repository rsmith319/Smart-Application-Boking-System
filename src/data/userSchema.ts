// Define the User interface to match the expected data structure
interface users {
  firstName: String;
  lastName: string;
  email: string;
  password: string;
  phoneNumber: string;
  role: string;
  createdAt: string;
  enabled: boolean;
  id: string;
  updatedAt: string;
}

export default users;